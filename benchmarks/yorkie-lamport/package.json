{
  "name": "yorkie-lamport-benchmarks",
  "version": "1.0.0",
  "description": "Yorkie Lamport benchmarks",
  "main": "./run.js",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "measure-bundle": "([ -f \"./dist/bundle.js\" ] || npm run dist) && node ../../bin/measure-bundle.js",
    "start": "npm run measure-bundle && node --expose-gc run.js && npm run table",
    "start:bun": "npm run measure-bundle && bun run run.js && npm run table",
    "start:browser": "rollup -c && 0serve -o benchmark.html",
    "start:ci": "npm run measure-bundle && node --expose-gc run.js",
    "table": "echo 'Yorkie Lamport results: \n\n' && node ../../bin/render-table.js ../results.json 6000 yorkie-lamport",
    "dist": "npm run clean && rollup -c && gzip --keep dist/*"
  },
  "license": "MIT",
  "dependencies": {
    "yorkie-js-sdk-lamport": "github:yorkie-team/yorkie-js-sdk#crdt-bench-lamport-v052"
  }
}
